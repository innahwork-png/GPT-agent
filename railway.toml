[build]
builder = "RAILPACK"

[deploy]
startCommand = "PYTHONPATH=src uvicorn personal_ai_team.main:app --host 0.0.0.0 --port $PORT"
healthcheckPath = "/health"
healthcheckTimeout = 120
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 5
